// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://renukasmakeover.com', // PLACEHOLDER — update once domain is finalized
});
